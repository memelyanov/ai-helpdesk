# API Contract & Error-Handling Checklist: Document Listing and Download Endpoints

**Purpose**: Validate that the requirements governing `GET /documents` and `GET /documents/{id}/content` — their status codes, response shapes, and error-handling rules — are complete, unambiguous, internally consistent, and objectively testable, before `/speckit-tasks` turns them into implementation work.
**Created**: 2026-08-15
**Feature**: [spec.md](../spec.md) | [contracts/document-query-api-contract.md](../contracts/document-query-api-contract.md) | [data-model.md](../data-model.md) | [research.md](../research.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements. Focus: API contract & error-handling requirement quality. Depth: standard.

## Requirement Completeness

- [x] CHK001 Are the exact HTTP status codes enumerated for every success and failure outcome of both endpoints, not just the "happy path"? [Completeness, Spec §FR-006, FR-010] — **Resolved** (2026-08-15): `GET /documents` → `200` (success) or an implementation-default server error (CHK002, an intentional, documented non-commitment, not a gap); `GET /documents/{id}/content` → `200` (success) or `404 document_not_found` (FR-010). Every outcome is now enumerated.
- [x] CHK002 Is a database/connectivity failure on `GET /documents` (as opposed to the documented empty-corpus case) assigned an explicit status code anywhere in the requirements? [Gap, Spec §FR-006] — **Resolved** (`/speckit-analyze` E2, 2026-08-15): FR-006 now states an empty list MUST only ever mean a genuinely empty corpus, and a new Edge Case requires an error response for any other failure to produce the list. The exact numeric status is still an implementation detail (plan.md: an unhandled read failure surfaces as the framework's ordinary server error), which is an intentional, documented choice, not a remaining gap.
- [x] CHK003 Is the full vocabulary of error codes shared across all three `/documents` endpoints (this feature's `document_not_found` plus feature 004's `400`/`503` codes) documented in one place a caller can rely on, rather than split silently across two features' docs? [Completeness, Traceability] — **Resolved** (2026-08-15, no edit needed): `data-model.md`'s `DocumentErrorResponse` table already lists all six codes (five `POST` codes plus `document_not_found`) with endpoint and cause in one table.
- [x] CHK004 Are response header requirements (`Content-Type`, `Content-Disposition`) for the download endpoint's **error** response specified with the same rigor as its success response? [Completeness, contracts/document-query-api-contract.md] — **Resolved** (2026-08-15): the contract doc's error response section now states explicitly that no `Content-Disposition` header is present on the `404`.
- [x] CHK005 Does a requirement state the error-response shape's field types (`error`: string, `message`: string), or is that shape only implied by example JSON in data-model.md/the contract doc? [Gap, Traceability] — **Resolved** (2026-08-15, no edit needed): confirmed this matches feature 004's own established pattern — `specs/004-document-ingestion-endpoint/spec.md` likewise never states the error DTO's field types in an FR; that shape lives in `data-model.md`/the contract doc for both features, a consistent documentation split (spec = behavior, data-model/contract = exact wire shape), not a defect specific to this feature.

## Requirement Clarity

- [x] CHK006 Is "malformed id" defined precisely enough (e.g., not a syntactically valid identifier) that two implementers would agree on the same set of inputs it covers? [Clarity, research.md Decision 4] — **Resolved** (`/speckit-analyze` C1, 2026-08-15): spec.md's Edge Cases now defines it directly ("not even validly formatted... for example, not a UUID"), not only research.md.
- [x] CHK007 Is the ordering guarantee in FR-005 ("most recently uploaded first") precise enough to be tested objectively, including the tie-break rule for two documents sharing an identical upload timestamp? [Measurability, Spec §FR-005] — **Resolved** (`/speckit-analyze` F1, 2026-08-15): FR-005 now requires a deterministic tiebreak for same-timestamp entries.
- [x] CHK008 Is it stated whether the `message` text in a `404 document_not_found` response is contractual (callers may match on it) or purely informational (subject to change without notice)? [Ambiguity, contracts/document-query-api-contract.md] — **Resolved** (2026-08-15): the contract doc now states `message` is informational only (mirroring feature 004's identical guidance for its own error responses) — the status code and `error` field are the stable contract.
- [x] CHK009 Is "clearly" in FR-010 ("MUST reject the request with a clear 'not found' outcome") tied to a specific, testable status code and body shape, rather than left as a subjective quality? [Clarity, Spec §FR-010] — **Resolved** (2026-08-15): FR-010 now states the malformed and nonexistent cases resolve to the identical outcome, and the contract doc already pins the exact status/body shape.

## Requirement Consistency

- [x] CHK010 Are `DocumentSummaryResponse`'s field names and types consistent between spec.md FR-002's prose description and data-model.md's table? [Consistency, Spec §FR-002] — **Resolved** (2026-08-15, no edit needed): verified 1:1 — FR-002's "identifier / original filename / content type / date-time uploaded / number of chunks" maps exactly to `documentId`/`filename`/`contentType`/`uploadedAt`/`chunkCount`.
- [x] CHK011 Are the error DTO's field names and the `document_not_found` code spelled identically across data-model.md's error table and the contract doc's example payload? [Consistency] — **Resolved** (2026-08-15, no edit needed): verified identical spelling (`error`, `message`, `document_not_found`) in both places.
- [x] CHK012 Are the contract doc's "Non-guarantees" (no pagination, no range downloads, no auth, no delete) also reflected as explicit constraints in spec.md's Assumptions, so a reader of either document alone reaches the same conclusion? [Consistency] — **Resolved** (2026-08-15): pagination/auth/delete were already covered; added a new "No partial/range downloads" Assumptions bullet to close the one gap (range downloads weren't previously mentioned in spec.md).
- [x] CHK013 Does the tie-break ordering rule (documented only in research.md Decision 2) belong in spec.md's FR-005 as well, given it is a caller-observable behavior rather than an implementation detail? [Consistency, Gap] — **Resolved** (`/speckit-analyze` F1, 2026-08-15): now stated in FR-005 and mirrored in the contract doc's ordering bullet.

## Error Handling & Status-Code Mapping

- [x] CHK014 Does every documented failure mode map to exactly one status code, with no scenario left where two different codes could plausibly apply? [Completeness, Spec §Edge Cases] — **Resolved** (2026-08-15): the new list-failure Edge Case and FR-006 clarification close the one previously-unmapped mode (a non-empty-corpus list failure).
- [x] CHK015 Is the requirement that a malformed id and a well-formed-but-nonexistent id produce the *identical* response (status, body, and headers) explicit enough to prevent an implementer from differentiating them in a response header or log-visible detail a caller could still observe? [Clarity, research.md Decision 4] — **Resolved** (2026-08-15): FR-010 now states this explicitly at the spec level, not only in research.md.
- [x] CHK016 Is it specified whether the constitution's Error Handling & Logging clause (4xx for invalid input, 5xx for valid-input-processing-failure) is satisfied by treating "not found" as a 404 rather than a 4xx-vs-5xx judgment call, given a nonexistent id is arguably "valid input, no matching resource" rather than "invalid input"? [Consistency, constitution.md Error Handling & Logging] — **Resolved** (2026-08-15): plan.md's Error Handling & Logging compliance note now spells out the reasoning — `404` is itself a `4xx`, and the "input" at issue is the requested `{id}`, which either doesn't resolve or isn't validly formatted; either way no retry of the identical request helps, the same distinction `4xx` exists to signal.

## Scenario & Edge Case Coverage

- [x] CHK017 Is the "already deleted" phrase in spec.md's Edge Cases reconciled with the Assumptions section's statement that no delete endpoint exists yet — is this phrase forward-looking/aspirational, or does it describe a reachable state today? [Ambiguity, Spec §Edge Cases, Spec §Assumptions] — **Resolved** (`/speckit-analyze` B1, 2026-08-15): the phrase is removed; the Edge Case is now scoped to "never ingested" with an explicit parenthetical noting no delete capability exists yet.
- [x] CHK018 Are requirements defined for a document identifier that is syntactically a valid UUID but uses unexpected casing or formatting (e.g., uppercase hex, missing hyphens)? [Edge Case, Gap] — **Resolved** (2026-08-15, no edit needed): already fully covered by FR-010's two branches without a new requirement — unexpected casing (e.g. uppercase hex) still parses to a valid UUID and is an ordinary lookup (PostgreSQL's `uuid` type compares by value, not string form), while missing hyphens or wrong length fails to parse at all and falls under FR-010's "not even a validly formatted identifier" branch. No third case exists.
- [x] CHK019 Are filename edge cases (empty string, extremely long, containing quote or control characters — feature 004 FR-017 stores filenames verbatim and unsanitized) addressed by a requirement on how the download endpoint's `Content-Disposition` header must behave, rather than left to the contract's prose alone? [Coverage, Gap, research.md Decision 3] — **Resolved** (`/speckit-analyze` C2, 2026-08-15): FR-009 now requires safe encoding of the filename in the response, mirrored in the contract doc's `Content-Disposition` bullet.
- [x] CHK020 Are concurrent-access scenarios (e.g., a download requested for a document mid-way through a hypothetical future delete) explicitly out of scope, or silently assumed away because no delete endpoint exists yet? [Edge Case, Spec §Assumptions] — **Resolved** (2026-08-15): the Assumptions' "Read-only against feature 003's schema" bullet now states explicitly that no concurrent-mutation race is possible today (no delete/update exists anywhere), deferring it to whichever future feature introduces mutation.

## Acceptance Criteria Quality

- [x] CHK021 Is SC-002's "100% byte-for-byte identical" claim paired with a defined verification method (e.g., checksum or full comparison) rather than left to an implementer's choice of proof? [Measurability, Spec §SC-002] — **Resolved** (2026-08-15, no edit needed): `quickstart.md` Step 5 already specifies the method (`Compare-Object` full-content comparison against the originally uploaded file).
- [x] CHK022 Is SC-003's "clear 'not found' outcome" criterion measurable independently of SC-003's own wording — i.e., does it point to the same status code/body defined elsewhere, or could a different-but-still-"clear" response also satisfy it? [Measurability, Spec §SC-003] — **Resolved** (2026-08-15): SC-003 now explicitly cross-references FR-010 ("the clear 'not found' outcome FR-010 defines") and is broadened to cover malformed ids too, matching FR-010's scope exactly.
- [x] CHK023 Is SC-001's 2-second target scoped precisely (server-side processing time vs. full round-trip including network) so a test can unambiguously pass or fail it? [Clarity, Spec §SC-001] — **Resolved** (2026-08-15): SC-001 now states it is measured as wall-clock time for the HTTP call against a locally running backend (quickstart.md Step 6), not server-internal-only processing time.

## Dependencies & Assumptions

- [x] CHK024 Is the assumption that `documents.content_type` is always exactly `text/plain` or `application/pdf` (relied on by FR-009 and the contract's `Content-Type` guarantee) traced to the schema constraint that actually enforces it, so a future schema change would be caught as a contract break? [Traceability, data-model.md, Spec §FR-009] — **Resolved** (2026-08-15, no edit needed): `data-model.md`'s `DocumentSummaryResponse` table already traces `contentType` to "schema `CHECK`, feature 003" explicitly.
- [x] CHK025 Is the dependency on feature 004's atomic, all-or-nothing ingestion (relied on by FR-004's "no partial entry ever appears") called out as an explicit cross-feature dependency rather than an implicit assumption a reader must infer? [Dependency, Spec §Edge Cases] — **Resolved** (2026-08-15, no edit needed): spec.md's Edge Cases already states this explicitly — "Because feature 004 guarantees a document only becomes visible once its full ingestion outcome has committed (all-or-nothing)..."

## Notes

- Check items off as completed: `[x]`
- This checklist intentionally does not duplicate `checklists/requirements.md` (the general completeness/clarity/consistency pass already run at spec stage) — every item here targets API contract shape, status-code mapping, and error-handling requirement quality specifically.
- Items flagging a `[Gap]` or `[Ambiguity]` are candidates for a spec.md/contract amendment before `/speckit-tasks`, not implementation TODOs.

### Resolution pass 1 (2026-08-15, post-`/speckit-analyze`)

11 of 25 items (CHK002, CHK006, CHK007, CHK009, CHK013–CHK017, CHK019, CHK021) were resolved this
pass via targeted edits to `spec.md`, `contracts/document-query-api-contract.md`, and `plan.md` —
see each item's resolution note above. `tasks.md` needed no change: it already tested the behaviors
these edits merely gave spec-level backing to.

### Resolution pass 2 (2026-08-15, pre-`/speckit-implement`)

The remaining 14 items (CHK001, CHK003–CHK005, CHK008, CHK010–CHK012, CHK018, CHK020, CHK022–CHK025)
were all resolved before implementation began, per the user's request. Six needed no artifact
change — verification confirmed the requirement was already adequately covered (CHK003, CHK005,
CHK010, CHK011, CHK018, CHK024, CHK025). Eight needed a small edit: two new `spec.md` Assumptions
bullets (no concurrent-mutation race possible; no partial/range downloads), an SC-001 measurement
scope clarification, an SC-003 cross-reference to FR-010 plus scope broadening to match it, and two
new bullets in the contract doc's error response section (no `Content-Disposition` on errors;
`message` is informational, not contractual).

**All 25 items in this checklist are now resolved: 25/25 `[x]`.**
